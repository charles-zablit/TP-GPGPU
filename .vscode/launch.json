{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "CUDA: Launch",
            "type": "cuda-gdb",
            "request": "launch",
            "program": "${workspaceFolder}/bin/ann_par",
            "preLaunchTask": "Build Parallel (Debug)",
        }
    ]
}